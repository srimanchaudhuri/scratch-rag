# scratch-rag
A complete retrieval-augmented generation (RAG) system where every component is my own code: tokenizer, chunker, embeddings, search index, ranker and a tiny language model. The goal is not answer quality. It is proof of understanding each layer well enough to build and measure it.
